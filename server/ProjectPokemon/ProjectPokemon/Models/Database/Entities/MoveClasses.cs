namespace ProjectPokemon.Models.Database.Entities {
    // Aquí se almacenan las clases que puede ser un movimiento
    public enum MoveClasses {
        status,
        physical,
        special
    }
}
